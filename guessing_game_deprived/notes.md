This is the deprived version of `guessing_game`. Since we couldn't download
the `rand` Crate from the internet, we wrote our own little random number generator
called `randgen.sh` using Bash. You can find it in the `src/` directory. To use it,
add it to your `PATH`.
